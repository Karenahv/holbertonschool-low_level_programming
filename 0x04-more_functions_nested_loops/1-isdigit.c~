#include "holberton.h"

/**
 * _isdigit - Entry point
 *@c: charater to check
 * Return: if is digit  or not, return 1 else 0
 */
int _isdigit(int c)
{

	if (isdigit(c))
		return (1);
	else
		return (0);
}
